# D-Day 계산기

GitHub Pages에서 바로 배포할 수 있는 정적 웹사이트입니다.

## 파일 구조

- `index.html` : 메인 페이지
- `assets/style.css` : 디자인
- `assets/app.js` : D-Day 계산 기능

## GitHub Pages 배포

1. GitHub에서 새 Repository를 생성합니다.
2. 이 폴더의 파일을 Repository에 업로드합니다.
3. Repository의 **Settings → Pages**로 이동합니다.
4. **Deploy from a branch**를 선택합니다.
5. Branch는 `main`, 폴더는 `/ (root)`를 선택합니다.
6. Save를 누르면 잠시 후 GitHub Pages 주소가 생성됩니다.

## 다음 확장

- `/cal/age/` 나이 계산기
- `/cal/percent/` 퍼센트 계산기
- `/cal/discount/` 할인율 계산기
- `/cal/vat/` 부가세 계산기
- `/tools/receipt/` 증빙자료 생성기
- 공통 헤더/푸터 및 관련 도구 링크
- Google Search Console / 네이버 서치어드바이저 등록
- AdSense 적용
